{
  "name": "app",
  "private": true,
  "version": "0.0.0",
  "description": "Lambdas to maintain and forward cloudwatch logs",
  "scripts": {
    "build": "tsc",
    "test": "jest"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@aws-sdk/client-cloudwatch-logs": "^3.858.0",
    "@aws-sdk/client-ecs": "^3.858.0",
    "@aws-sdk/client-kinesis": "^3.858.0",
    "@aws-sdk/client-lambda": "^3.858.0",
    "@aws-sdk/client-s3": "^3.858.0"
  },
  "jest": {
    "testMatch": [
      "<rootDir>/src/**/*.test.ts"
    ],
    "transform": {
      "^.+\\.tsx?$": "ts-jest"
    }
  }
}
